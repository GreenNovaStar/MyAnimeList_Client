/// Which MAL media type a list/search operates on. Drives endpoint choice
/// (animelist vs mangalist, /anime vs /manga) and per-type UI (status tabs,
/// labels, detail navigation).
enum MediaKind { anime, manga }
